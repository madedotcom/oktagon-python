name: Release



on:
  pull_request:
    types: closed

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get-version.outputs.version }}
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"
      - name: Install deps
        run: |
          python -m pip install pip --upgrade
          pip install githubrelease
          pip install autopub
      - name: Check if we should release
        id: check_release
        run: |
          set +e
          echo ::set-output name=release::$(autopub check)
      - name: Publish
        if: steps.check_release.outputs.release == ''
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: pypi-AgEIcHlwaS5vcmcCJGY3ZWY2OGM1LTlhYzAtNGJiZS04Yjg2LTFkMGJlN2VhZjc4OQACP3sicGVybWlzc2lvbnMiOiB7InByb2plY3RzIjogWyJva3RhZ29uLXB5dGhvbiJdfSwgInZlcnNpb24iOiAxfQAABiBim9A1kjA-1Fdlw-46u_9KMGuYWIvt88YQKBX97MpSSw
        run: |
          autopub prepare
          autopub build
          autopub commit
          autopub githubrelease
          make publish
      - name: Get project version
        id: get-version
        shell: python
        run: |
          from autopub.base import get_project_version
          print(f"::set-output name=version::{get_project_version()}")
